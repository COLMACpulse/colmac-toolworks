# colmacpulse.com — Edit Punch List

The spotlight build is done: all 30 products open to a full spotlight (29 hero renders + VIRIDENCE on its generated deviation panel). What's below is what still needs a **decision, a wire-in, or a cleanup** before this is launch-ready. Everything lives in `index.html` unless noted.

---

## 1. Product identity — RESOLVED
You sent renders that locked each tool's identity. Done:
- StoryForge -> TV treatments / season structures (reverted).
- SignalScript -> dictation & transcript cleaner (reverted).
- AUTHORPRINT -> rewrite-into-your-voice (reverted; demo matches again).
- Script Surgeon -> screenplay revision control (reverted).
- Writer's Room -> kept collaborative (the render you made is collaborative). Only open sub-question: a multi-member room vs your "offline, local only" line.
- **MAG DUMP + MAG FINDER -> moved from Engines to the Writers' Bench.** Mag Dump = score a story idea for project fit; Mag Finder = match your story to magazines/festivals. See #2 for the pricing this triggered.


## 2. Commerce — wire before launch

**Pack pricing I set when the two MAG tools moved (confirm or change):**
- Operator Pack: now **9 file engines**, kept at **$199** (was $199/eleven). "instead of" sum corrected to **$391**.
- Writers' Bench pack: now **8 tools**, set to **$229 instead of $442**. (Was $149/"five.")
- Both are my picks under "price accordingly" — change either in one word.

- **Buy links (all empty).** Every product's `buy:""` is blank, so each shows "Link pending." Paste each Lemon Squeezy checkout URL into its `buy:""` as listings go live. Also the two packs: `PACK.buy` (Operator Pack) and `FILMPACK.buy` (Writers' Bench).
- **Prices to confirm.** VIRIDENCE is set to $49 (you floated $29–39 earlier). Spot-check the rest: most are $49; Local Retrieval / SignalScript are $29; Script Surgeon $99; StoryForge $69.
- **Pack math.** Operator Pack still says **"all eleven file engines, $199 instead of $489."** Confirm the count and the math now that the engine line is finalized at 11. Writers' Bench pack says **"$149 instead of $295."**

---

## 3. Content / asset cleanups
- **AUTHORPRINT demo contradicts its render.** The embedded demo (`authorprint-demo.html`) is the OLD de-AI tool; the render + new copy are the author-DNA profiler. They disagree in the same spotlight. Either rebuild the demo to match, or drop the demo until it does.
- **GROUNDSCOPE render — FIXED.** Clean version is in (wordmark correct, subtitle clean). This item is closed.
- **Lead copy.** The intro line now reads "...the writers' bench for film and TV, and VIRIDENCE for the grow." Confirm that framing still fits the four lines.

---

## 4. Pre-counsel / housekeeping (your standing AUTOSCOPE rule)
- Register **viridence.com** and file the software / ag trademark class before counsel or buyers.
- Collision-check any scope names you have NOT shipped yet before they go live.

---

## 5. Optional / deferred
- **VIRIDENCE Gold Run v2** — the clean non-cannabis lifecycle shoot (capture spec already delivered). Still deferred.
- **VIRIDENCE render.** It's the only product on the generated deviation panel instead of a hero infographic — intentional, since it *is* the ribbon tool. Make a render only if you want it to match the family look.

---

### How to hand edits back
- **Copy / sub change:** tell me the product name + the new text, I wire it.
- **Buy link:** paste product name + Lemon Squeezy URL.
- **New/replacement render:** send the image, name the product.
